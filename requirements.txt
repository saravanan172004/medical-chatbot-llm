# Open and overwrite requirements.txt completely
cat > requirements.txt << 'EOF'
flask
langchain
langchain-community
langchain-pinecone
langchain-google-genai
google-generativeai
pinecone
python-dotenv
sentence-transformers
pypdf
langchain-core
gunicorn
EOF

# Save and push
git add requirements.txt
git commit -m "clean requirements no versions no -e"
git push origin main
